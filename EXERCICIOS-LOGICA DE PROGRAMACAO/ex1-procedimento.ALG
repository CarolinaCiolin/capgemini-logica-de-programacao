Algoritmo "Procedimento"

Var

resultado: inteiro //variavel global

procedimento somar(i, j: inteiro) //variaveis locais
inicio
      resultado <- i + j
fimprocedimento

Inicio

somar(10,20)
escreva(resultado)


Fimalgoritmo